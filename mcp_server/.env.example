# MCP Server Environment Variables
# Copy this file to .env and fill in your actual values
#
# All APIs are optional - the server will use mock data if credentials are not provided

# =============================================================================
# Transit APIs (Optional)
# =============================================================================
# Transit adapter will try APIs in this order:
# 1. Google Maps Directions API (best transit support - recommended)
# 2. OpenRouteService (limited transit support, uses driving as proxy)
# 3. Mock data (fallback if no API keys provided)

# Google Maps Directions API for transit route planning
# Provides real-time transit schedules, routes, and walking directions
# Get API key from: https://console.cloud.google.com/
# Enable "Directions API" in your Google Cloud project
GOOGLE_MAPS_API_KEY=

# OpenRouteService API (alternative transit/route planning)
# Limited transit support - uses driving directions with transit factors
# Get free API key from: https://openrouteservice.org/dev/#/signup
ORS_API_KEY=

# =============================================================================
# Lime GBFS (Optional - has default)
# =============================================================================
# Lime bike/scooter data feed URL
# Default: https://data.lime.bike/api/partners/v1/gbfs/san_francisco/gbfs.json
# Uncomment and change if you want a different location:
# LIME_GBFS_URL=https://data.lime.bike/api/partners/v1/gbfs/san_francisco/gbfs.json

