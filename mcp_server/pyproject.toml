[project]
name = "mcp-server"
version = "0.1.0"
description = "MCP server for route planning provider data"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=0.9.0",
    "httpx>=0.27.0",
    "pydantic>=2.9.0",
    "pydantic-settings>=2.5.0",
]

